{
  "name": "sgup",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "blocks": "wp-scripts build",
    "start": "wp-scripts start",
    "build": "npm run blocks && ./build.sh"
  },
  "devDependencies": {
    "@wordpress/scripts": "^31.1.0",
    "esbuild": "latest"
  }
}